En public class Usuario

se me ocurre para el 4a:

private List<Genero>generosVistos;

public void generosVistosSinRepeticion(){
       this.generosVistos.stream().distinct().collect(Collectors.toSet());
    }

